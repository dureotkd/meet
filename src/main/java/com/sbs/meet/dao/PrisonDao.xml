<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.sbs.meet.dao.PrisonDao">
	
	<insert id="register" useGeneratedKeys="true" keyProperty="id">
	INSERT INTO prison
	SET regDate = NOW(),
	updateDate = NOW(),
	nickname = #{nickname},
	tier = #{tier},
	memberId = 1
	</insert>
	
	<select id="getForPrintPrison" resultType="Prison">
	SELECT * FROM prison
	WHERE nickname LIKE '%#{searchKeyword}%'
	</select>

</mapper>